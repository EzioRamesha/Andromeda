@model WebApp.Models.Mfrs17ReportingViewModel

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body>
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Mfrs17ReportingViewModel</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @*@Html.DisplayNameFor(model => model.ReportingQuarter)*@
            </dt>

            <dd>
                @*@Html.DisplayFor(model => model.ReportingQuarter)*@
            </dd>

            <dt>
                @*@Html.DisplayNameFor(model => model.Status)*@
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Status)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.TotalRecord)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.TotalRecord)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color form-row">
                <div class="form-group col-md-12 btn-row">
                    @Html.ActionLink("CANCEL", "Index", null, new { @class = "btn btn-cancel" })
                    <button type="submit" class="btn btn-del float-right">DELETE</button>
                </div>
            </div>
        }
    </div>
</body>
</html>
